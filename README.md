# Rld

## librld

Librld is the core of the rld project; it is distributed as a shared object and a collection of headers. It is more of a framework than a library because it takes over the entire application process, and you need to provide implementation for called function.

## rld script

Rld script is a bash script that can installed separately from librld and is used for bootstraping and running rld in your project.
