## Rld basic example

Basic example of rld usage. It is just one c file that gets compiled and run. 

First run `rld run`, after that you can start changing the contents of the `main.c` and observe changes.